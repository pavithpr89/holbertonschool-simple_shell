#include "shell.h"

/**
 * main - Entry point
 * @argc: Argument count
 * @argv: Argument vector
 *
 * Return: Always 0
 */
char *find_command(char *cmd)
{

  char full_path[64];
  char *path, *path_copy, *dir; 


  if (strchr(cmd, '/')) 
  {
    

    return (NULL);
  }








}

